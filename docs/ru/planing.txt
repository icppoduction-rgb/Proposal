Этап первый сбор датасетов
Этап второй распределение датасетов по папкам TRAIN | VALIDATION | TEST | EXPERIMENTS
Этап третий запись всех путей к файлам датасетов в json:
{ dns: { Train: [], VALIDATION: [], TEST: []}, host: {Train: [], VALIDATION: [], TEST: [], EXPERIMENTS: []} }
Этап четвертый конвертация основной документации в .md
Этап пятый выборка данных сортировка датасетов
Этап шестой преобразование датасетов разных форматов в единный формат json

# -------------------------------- Задачи -------------------------- #

Задача 1:
- проанализировать файл "docs/ru/project_proposal_analysis.md" основной упор нужно сделать на модели и датасеты
- проанализируй файлы "docs/ru/dns_dataset_strategy.md", "docs/ru/host_datasets_analysis.md"
- про анализируй файлы "data-json/dataset-path-data.json", 'data-json/extensions.json'
  Описание:
    - dataset-path-data.json - содержит пути к dataset файлам
    - extensions - содержит расширение файлов

Задача 2:
- на основе проанализированных файлов и их расширений нужно сделать выборку датасетов, нужных и ненужных для обучения
  Задача 2.1:
    - выбери нужные датасеты и запиши пути в файл "dataset-processed-path.json" а расширения в "dataset-processed-extensions.json"
    - не нужные датасеты нужно удалить из "datasets/"
    - используй для этого python скрипты, написанные скрипты для этой задачи должны быть в "scripts/"
    - запуск скриптов через "manage.py" - проанализируй файл "manage.py" изменять запрещено, добавлять новые команды можно

Рекомендации к коду:
- код должен быть написан грамотно
- комментарии к каждой функции должны быть понятны

Задача 3:
- проанализировать еще раз все папка с datasets весит ~400 ГБ это слишком большой набор данных для дипломной работы
- проанализируй "dataset-processed-extensions.json"
  Задача 3.1:
  - нужно исключить и удалить те файлы которых формат прочесть невозможно использую python
  - обновить "dataset-processed-path.json", "dataset-processed-extensions.json" после исключения и удаление файлов в datasets


Задача 4
Требуется провести анализ репозитория и реализовать универсальный механизм чтения файлов различных форматов.
Требования:
1. Проанализировать структуру и код репозитория.
   - Папку `datasets` исключить из анализа.
   - Анализ должен охватывать только рабочий код и связанные конфигурации.
2. Проанализировать файл:
   - `data-json/dataset-processed-extensions.json`
   - определить список поддерживаемых расширений файлов.
3. Проанализировать:
   - `data-json/dataset-processed-path.json`
   - а также все файлы, пути к которым указаны внутри данного JSON.
4. Реализовать скрипт:
   - название файла: `scripts\read_format.py`
5. Назначение скрипта:
   - скрипт должен уметь читать файлы всех форматов, перечисленных в:
     `data-json/dataset-processed-extensions.json`
6. Логика работы:
   - в соответствующий метод передается путь к файлу.
   - пример:
     ```python
     read_file("path/test.csv")
     ```
   - результат работы:
     ```python
     {
         "data": ...
     }
     ```
   - возвращаемое значение должно быть словарем с содержимым файла в унифицированном формате.
7. Дополнительные требования:
   - для каждого формата должна быть отдельная логика обработки;
   - необходимо предусмотреть обработку ошибок:
     - файл не найден;
     - неподдерживаемый формат;
     - ошибка чтения файла;
   - код должен быть расширяемым для добавления новых форматов в будущем;
   - желательно использовать единый интерфейс чтения файлов.


Задача

Доработать скрипт `read_format.py`.

Требования к реализации:

1. Код должен быть написан в стиле ООП.

2. Реализация должна быть простой, понятной и рабочей.

3. Добавить комментарии к каждой функции и методу.

4. Убрать из кода следующие константы:
   ```python
   PROJECT_ROOT = Path(__file__).resolve().parents[1]
   DEFAULT_EXTENSIONS_FILE = PROJECT_ROOT / "data-json" / "dataset-processed-extensions.json"

