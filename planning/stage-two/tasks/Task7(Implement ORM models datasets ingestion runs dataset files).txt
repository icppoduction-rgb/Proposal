================================================================================

0. ОБЩИЕ ПРАВИЛА ДЛЯ ВСЕХ ЗАДАЧ
================================================================================

1. Работать малыми логически завершенными задачами.
2. После каждой задачи создавать два отчета:
   - PATH_REPORT/ru/stage-two/Task<number>(<русское название>).md
   - PATH_REPORT/en/stage-two/Task<number>(<english name>).md
3. Отчеты создавать до git add / git commit / git push.
4. Каждый отчет должен содержать:
   - цель задачи;
   - выполненные изменения;
   - измененные/созданные файлы;
   - команды проверки;
   - результат проверок;
   - commit message;
   - статус git push;
   - проблемы/ограничения;
   - следующий шаг.
5. Код должен использовать type hints.
6. Каждый новый модуль должен иметь module docstring.
7. Каждый публичный класс и метод должен иметь docstring.
8. Комментарии добавлять только для нетривиальной логики.
9. Не хранить реальные credentials.
10. Не коммитить .env.
11. Не изменять raw datasets.
12. Не читать большие raw files в DB foundation задачах.
13. Если поле отсутствует в датасете, сохранять NULL / None.
14. Не смешивать TRAIN, VALIDATION и TEST.
15. TEST нельзя использовать для fit scaler/encoder/imputer/feature selector/model.
16. PostgreSQL не использовать для хранения больших normalized events/features.
17. Parquet использовать для normalized/features/model-ready artifacts.
18. DuckDB использовать для SQL analytics over Parquet.
19. Базовые проверки после каждой задачи:
   - python -m compileall manage.py scripts
   - git diff --check
20. Если добавлены тесты, запускать project test runner.

================================================================================
ЗАДАЧА 7. Реализовать ORM models: datasets, ingestion_runs, dataset_files
================================================================================

Цель:
Описать raw/catalog входной слой.

Что сделать:
1. Создать models:
   - Dataset
   - IngestionRun
   - DatasetFile
2. Реализовать все колонки из PostgreSQL catalog schema.
3. Добавить CHECK constraints for role/branch/status.
4. Добавить UNIQUE(dataset_id, file_path).
5. Добавить indexes.
6. Добавить relationships.

Acceptance criteria:
- Models импортируются.
- Constraints есть.
- dataset_files поддерживает raw file metadata.
- compileall и git diff --check проходят.

git add .
git commit -m "Add dataset ingestion and file catalog models"
git push origin dev
