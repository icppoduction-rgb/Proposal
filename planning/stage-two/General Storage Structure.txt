ОБЩАЯ СТРУКТУРА ХРАНЕНИЯ ЧЕРЕЗ PATH_DATA_STORAGE

# ==========================
${PATH_DATA_STORAGE}/
├── postgres/
├── pgadmin/
├── parquet/
│   ├── normalized/
│   ├── features/
│   └── model_ready/
├── duckdb/
├── reports/
├── logs/
├── backups/
└── temp_data/
# ==========================

.env
# ==========================
PATH_DATA_STORAGE=C:\Users\Public\PythonProjects\storage

POSTGRES_DB=proposal_catalog
POSTGRES_USER=proposal_user
POSTGRES_PASSWORD=proposal_password
POSTGRES_PORT=5432

PGADMIN_PORT=5050
PGADMIN_DEFAULT_EMAIL=admin@example.com
PGADMIN_DEFAULT_PASSWORD=admin
# ==========================


docker-compose.yml
# ==========================
services:
  postgres:
    image: postgres:16
    container_name: proposal-postgres
    restart: unless-stopped
    environment:
      POSTGRES_DB: ${POSTGRES_DB}
      POSTGRES_USER: ${POSTGRES_USER}
      POSTGRES_PASSWORD: ${POSTGRES_PASSWORD}
    ports:
      - "${POSTGRES_PORT}:5432"
    volumes:
      - postgres_data:/var/lib/postgresql/data
    healthcheck:
      test: ["CMD-SHELL", "pg_isready -U $${POSTGRES_USER} -d $${POSTGRES_DB}"]
      interval: 10s
      timeout: 5s
      retries: 5

  pgadmin:
    image: dpage/pgadmin4:8
    container_name: proposal_pgadmin
    restart: unless-stopped
    depends_on:
      postgres:
        condition: service_healthy
    environment:
      PGADMIN_DEFAULT_EMAIL: ${PGADMIN_DEFAULT_EMAIL}
      PGADMIN_DEFAULT_PASSWORD: ${PGADMIN_DEFAULT_PASSWORD}
    ports:
      - "${PGADMIN_PORT}:80"
    volumes:
      - pgadmin_data:/var/lib/pgadmin

volumes:
  postgres_data:
    driver: local
    driver_opts:
      type: none
      o: bind
      device: ${PATH_DATA_STORAGE}/postgres

  pgadmin_data:
    driver: local
    driver_opts:
      type: none
      o: bind
      device: ${PATH_DATA_STORAGE}/pgadmin
# ==========================
