Проект: Proposal - Behaviour-driven hybrid learning for data exfiltration detection
Этап: Stage Two - Data Normalization
Дата подготовки: 2026-06-13
Формат: TXT


ОБНОВЛЕННЫЕ ЗАДАЧИ ДЛЯ CODEX: ПОЛНАЯ РЕАЛИЗАЦИЯ STAGE TWO DATA NORMALIZATION
================================================================================

0. ОБЩИЕ ПРАВИЛА ДЛЯ ВСЕХ ЗАДАЧ
================================================================================

1. Работать малыми логически завершенными задачами.
2. После каждой задачи создавать два отчета:
   - PATH_REPORT/ru/stage-two/Task<number>(<русское название>).md
   - PATH_REPORT/en/stage-two/Task<number>(<english name>).md
3. Отчеты создавать до git add / git commit / git push.
4. Каждый отчет должен содержать:
   - цель задачи;
   - выполненные изменения;
   - измененные/созданные файлы;
   - команды проверки;
   - результат проверок;
   - commit message;
   - статус git push;
   - проблемы/ограничения;
   - следующий шаг.
5. Код должен использовать type hints.
6. Каждый новый модуль должен иметь module docstring.
7. Каждый публичный класс и метод должен иметь docstring.
8. Комментарии добавлять только для нетривиальной логики.
9. Не хранить реальные credentials.
10. Не коммитить .env.
11. Не изменять raw datasets.
12. Не читать большие raw files в DB foundation задачах.
13. Если поле отсутствует в датасете, сохранять NULL / None.
14. Не смешивать TRAIN, VALIDATION и TEST.
15. TEST нельзя использовать для fit scaler/encoder/imputer/feature selector/model.
16. PostgreSQL не использовать для хранения больших normalized events/features.
17. Parquet использовать для normalized/features/model-ready artifacts.
18. DuckDB использовать для SQL analytics over Parquet.
19. Базовые проверки после каждой задачи:
   - python -m compileall manage.py scripts
   - git diff --check
20. Если добавлены тесты, запускать project test runner.

================================================================================
ЗАДАЧА 1. Проверить storage foundation и обновить .env.example
================================================================================

Цель:
Подготовить конфигурацию Stage Two storage, PostgreSQL, SQLAlchemy и reports.

Что сделать:
1. Проверить наличие .env.example.
2. Добавить/проверить переменные:
   - PATH_DATA_STORAGE
   - PATH_FOLDER_DATASETS
   - PATH_FOLDER_DATASETS_FILTER
   - PATH_REPORT
   - PATH_TEMP_DATA
   - POSTGRES_DB
   - POSTGRES_USER
   - POSTGRES_PASSWORD placeholder
   - POSTGRES_PORT
   - DATABASE_URL
   - PGADMIN_PORT
   - PGADMIN_DEFAULT_EMAIL
   - PGADMIN_DEFAULT_PASSWORD placeholder
3. Добавить комментарий, что .env нельзя коммитить.
4. Не добавлять реальные credentials.
5. Не менять локальный .env.

Acceptance criteria:
- .env.example содержит DATABASE_URL.
- .env.example содержит PATH_DATA_STORAGE.
- .env.example не содержит production secrets.
- compileall и git diff --check проходят.

Commit:
Add Stage Two environment configuration

================================================================================
ЗАДАЧА 2. Создать/обновить storage bootstrap utility
================================================================================

Цель:
Автоматически создавать directories внутри PATH_DATA_STORAGE.

Что сделать:
1. Создать scripts/stage_two/storage/.
2. Реализовать StorageBootstrapper class.
3. Создавать директории:
   - postgres, pgadmin
   - parquet/normalized, parquet/features, parquet/model_ready
   - duckdb/sql, duckdb/exports
   - reports/ru/stage-two, reports/en/stage-two
   - reports/parser, reports/normalization, reports/quality, reports/leakage, reports/schema_mismatch
   - logs/stage-two
   - backups/postgres_catalog
   - temp_data/ingestion, temp_data/parser_runs, temp_data/normalization
   - schemas/normalized, schemas/features, schemas/model_ready
   - config
4. Добавить CLI route или management function, если текущая архитектура это поддерживает.
5. Не удалять существующие файлы.

Acceptance criteria:
- Bootstrapper создает директории idempotently.
- Повторный запуск безопасен.
- compileall и git diff --check проходят.

Commit:
Add Stage Two storage bootstrapper

================================================================================
ЗАДАЧА 3. Добавить зависимости Stage Two DB foundation
================================================================================

Цель:
Добавить зависимости для PostgreSQL catalog.

Что сделать:
1. Обновить requirements.txt.
2. Добавить:
   - sqlalchemy
   - psycopg[binary]
   - alembic
3. Если python-dotenv уже есть, не дублировать.
4. Не добавлять ML/DL зависимости.
5. Не добавлять pandas/pyarrow/duckdb в этой задаче.

Acceptance criteria:
- requirements.txt содержит sqlalchemy, psycopg[binary], alembic.
- Нет дубликатов.
- compileall и git diff --check проходят.

Commit:
Add SQLAlchemy Alembic and PostgreSQL dependencies

================================================================================
ЗАДАЧА 4. Создать scripts/db config/session layer
================================================================================

Цель:
Подготовить DB access foundation.

Что сделать:
1. Создать scripts/db/__init__.py.
2. Создать scripts/db/config.py.
3. Создать scripts/db/session.py.
4. config.py должен читать DATABASE_URL.
5. session.py должен создать engine/sessionmaker/session_scope.
6. Реализовать rollback on exception.
7. Не создавать таблицы вручную.

Acceptance criteria:
- DB config импортируется.
- session_scope доступен.
- Нет hardcoded credentials.
- compileall и git diff --check проходят.

Commit:
Add SQLAlchemy configuration and session layer

================================================================================
ЗАДАЧА 5. Настроить Alembic migrations
================================================================================

Цель:
Подготовить migration layer.

Что сделать:
1. Создать scripts/db/migrations или стандартную alembic структуру.
2. Настроить alembic.ini / env.py.
3. Подключить metadata из SQLAlchemy Base.
4. DATABASE_URL брать из config/env.
5. Не создавать business tables в этой задаче.

Acceptance criteria:
- alembic current/head работает на пустой конфигурации.
- env.py использует project metadata.
- compileall и git diff --check проходят.

Commit:
Configure Alembic for PostgreSQL catalog migrations

================================================================================
ЗАДАЧА 6. Создать SQLAlchemy Base и naming convention
================================================================================

Цель:
Подготовить Base для стабильных constraints and migrations.

Что сделать:
1. Создать scripts/db/models/base.py.
2. Определить MetaData naming_convention.
3. Создать Declarative Base.
4. Добавить __init__.py exports.

Acceptance criteria:
- Base доступен для Alembic.
- naming_convention есть для ix/uq/ck/fk/pk.
- compileall и git diff --check проходят.

Commit:
Add SQLAlchemy declarative base with naming convention

================================================================================
ЗАДАЧА 7. Реализовать ORM models: datasets, ingestion_runs, dataset_files
================================================================================

Цель:
Описать raw/catalog входной слой.

Что сделать:
1. Создать models:
   - Dataset
   - IngestionRun
   - DatasetFile
2. Реализовать все колонки из PostgreSQL catalog schema.
3. Добавить CHECK constraints for role/branch/status.
4. Добавить UNIQUE(dataset_id, file_path).
5. Добавить indexes.
6. Добавить relationships.

Acceptance criteria:
- Models импортируются.
- Constraints есть.
- dataset_files поддерживает raw file metadata.
- compileall и git diff --check проходят.

Commit:
Add dataset ingestion and file catalog models

================================================================================
ЗАДАЧА 8. Реализовать ORM models: parser_registry, schema_versions, parser_runs
================================================================================

Цель:
Описать parser routing and parser run history.

Что сделать:
1. Создать models:
   - ParserRegistry
   - SchemaVersion
   - ParserRun
2. Реализовать constraints.
3. parser_runs.file_id -> dataset_files.id.
4. parser_runs.parser_registry_id -> parser_registry.id.
5. parser_runs.schema_version_id -> schema_versions.id.
6. Добавить lookup indexes.

Acceptance criteria:
- Parser routing metadata выражена в models.
- Parser run history связана с dataset_files.
- compileall и git diff --check проходят.

Commit:
Add parser registry schema version and parser run models

================================================================================
ЗАДАЧА 9. Реализовать ORM models: artifacts, preprocessing, labels, quality
================================================================================

Цель:
Описать artifact registry, label rules, preprocessing artifacts and quality reports.

Что сделать:
1. Создать models:
   - NormalizedArtifact
   - FeatureArtifact
   - ModelReadyArtifact
   - PreprocessingArtifact
   - LabelMappingRule
   - DataQualityReport
2. Реализовать FK links:
   - normalized -> dataset/file/parser_run/schema
   - feature -> dataset/normalized
   - preprocessing -> feature
   - model_ready -> feature/preprocessing
3. Добавить CHECK preprocessing fitted_on_role='TRAIN'.
4. Добавить constraints role/branch/status/data_type.

Acceptance criteria:
- Traceability chain выражена на уровне моделей.
- TEST cannot be fitted_on_role for preprocessing.
- compileall и git diff --check проходят.

Commit:
Add artifact label preprocessing and quality catalog models

================================================================================
ЗАДАЧА 10. Создать первую Alembic migration catalog schema
================================================================================

Цель:
Создать PostgreSQL catalog tables воспроизводимо.

Что сделать:
1. Создать Alembic revision.
2. Проверить autogenerate вручную.
3. Убедиться, что migration содержит:
   - tables
   - FK
   - CHECK
   - UNIQUE
   - indexes
4. Применить alembic upgrade head на local dev DB.
5. Не добавлять seed data в migration, если это не schema-level константы.

Acceptance criteria:
- alembic upgrade head успешен.
- alembic downgrade -1 работает.
- Все tables созданы.
- compileall и git diff --check проходят.

Commit:
Create initial Stage Two catalog schema migration

================================================================================
ЗАДАЧА 11. Реализовать repository layer
================================================================================

Цель:
Создать безопасный слой доступа к catalog.

Что сделать:
1. Создать scripts/db/repositories/.
2. Реализовать:
   - BaseRepository
   - DatasetRepository
   - IngestionRepository
   - DatasetFileRepository
   - ParserRepository
   - SchemaRepository
   - ArtifactRepository
   - LabelRepository
   - PreprocessingRepository
   - DataQualityRepository
3. Не коммитить прямые DB операции в parser classes.

Acceptance criteria:
- Repositories принимают Session.
- Нет создания engine внутри repositories.
- compileall и git diff --check проходят.

Commit:
Add PostgreSQL catalog repository layer

================================================================================
ЗАДАЧА 12. Реализовать smoke tests для DB foundation
================================================================================

Цель:
Проверить catalog schema and repositories.

Что сделать:
1. Добавить минимальные tests или smoke script.
2. Проверить:
   - create dataset
   - create ingestion_run
   - insert dataset_file
   - unique constraint
   - check constraint
   - parser_run
   - normalized artifact
   - preprocessing artifact TRAIN-only
   - data_quality_report
3. Не использовать реальные raw datasets.

Acceptance criteria:
- Smoke test проходит на test DB.
- Rollback работает.
- compileall и git diff --check проходят.

Commit:
Add PostgreSQL catalog smoke checks

================================================================================
ЗАДАЧА 13. Реализовать catalog ingestion service
================================================================================

Цель:
Сканировать raw/sorted dataset directories and register files in PostgreSQL.

Что сделать:
1. Создать scripts/stage_two/ingestion/.
2. Реализовать DatasetFileScanner.
3. Реализовать FileHashService.
4. Реализовать CatalogIngestionService.
5. Поддержать PATH_FOLDER_DATASETS and PATH_FOLDER_DATASETS_FILTER.
6. Определять dataset_name, branch, role, source_format.
7. Считать sha256, file size, modified time.
8. Использовать bulk upsert через SQLAlchemy Core.
9. Не читать содержимое больших файлов.

Acceptance criteria:
- Files регистрируются в dataset_files.
- Повторный запуск idempotent.
- ingestion_runs получает SUCCESS/PARTIAL_SUCCESS/FAILED.
- compileall и git diff --check проходят.

Commit:
Implement Stage Two catalog ingestion service

================================================================================
ЗАДАЧА 14. Реализовать parser registry seed and resolver
================================================================================

Цель:
Подготовить routing parser-а по branch/role/source_format.

Что сделать:
1. Создать config/parser_registry_seed.json.
2. Описать parsers for DNS csv/pcap/pcap.csv/txt and Host csv/log/json/txt/sc/ghc/bson/cap/pcap/pcapng/netflow_day/wls_day.
3. Реализовать ParserResolver.
4. Resolver должен искать parser по branch + role + source_format.
5. Если parser не найден, file status UNSUPPORTED_FORMAT or SKIPPED.

Acceptance criteria:
- parser_registry можно seed/update.
- ParserResolver возвращает parser metadata.
- Unsupported format не ломает pipeline.
- compileall и git diff --check проходят.

Commit:
Add parser registry seed and resolver

================================================================================
ЗАДАЧА 15. Реализовать normalized schema contracts
================================================================================

Цель:
Зафиксировать canonical normalized event schema.

Что сделать:
1. Создать schemas/normalized/normalized_event_v1.json или equivalent.
2. Описать базовые поля:
   event_uid, dataset_id, file_id, dataset_role, branch, source_format,
   parser_name, parser_version, timestamp, timestamp_source, timestamp_type,
   event_index, entity_type, entity_id, event_type, modality, label fields,
   features_json, raw_fields_json, metadata_json.
3. Зарегистрировать schema_versions.
4. Описать NULL policy.

Acceptance criteria:
- Schema contract есть в repository.
- schema_versions содержит normalized schema.
- NULL policy documented.
- compileall и git diff --check проходят.

Commit:
Add normalized event schema contract

================================================================================
ЗАДАЧА 16. Реализовать Parquet writer foundation
================================================================================

Цель:
Подготовить запись normalized/features/model-ready Parquet artifacts.

Что сделать:
1. Добавить зависимости pyarrow and/or polars only in this task if needed.
2. Создать scripts/stage_two/parquet/.
3. Реализовать ParquetArtifactWriter.
4. Поддержать paths:
   - parquet/normalized/{branch}/{role}/{modality}/{dataset_slug}/schema={version}/...
   - parquet/features/{feature_group}/{role}/{dataset_slug}/schema={version}/...
   - parquet/model_ready/...
5. Не писать в PostgreSQL сами events/features.
6. Регистрировать artifact path through ArtifactRepository.

Acceptance criteria:
- Writer создает partitioned paths.
- Missing fields сохраняются as NULL.
- Artifact registry получает row_count and path.
- compileall и git diff --check проходят.

Commit:
Add Parquet artifact writer foundation

================================================================================
ЗАДАЧА 17. Реализовать базовые parser interfaces
================================================================================

Цель:
Создать единый parser contract.

Что сделать:
1. Создать scripts/stage_two/parsers/.
2. Реализовать BaseParser abstract class.
3. Реализовать ParserResult dataclass.
4. ParserResult должен содержать rows_read, rows_parsed, rows_failed, events, warnings.
5. Parser должен отдавать normalized event dicts matching schema.
6. Parser не должен напрямую commit в DB.

Acceptance criteria:
- BaseParser есть.
- ParserResult есть.
- Parser contract documented.
- compileall и git diff --check проходят.

Commit:
Add Stage Two parser interface contract

================================================================================
ЗАДАЧА 18. Реализовать DNS normalization parsers MVP
================================================================================

Цель:
Нормализовать DNS csv/txt/pcap.csv MVP; pcap может быть stub/custom parser task.

Что сделать:
1. Реализовать DNS CSV parser with schema-aware handling.
2. Реализовать DNS TXT domain-list parser.
3. Реализовать DNS pcap.csv parser.
4. Headerless DNS TEST CSV поддержать positional schema.
5. Missing values -> NULL.
6. Labels through LabelResolver, not hardcoded in feature columns.

Acceptance criteria:
- DNS normalized events write to Parquet.
- TRAIN/VALIDATION/TEST separated.
- parser_runs and normalized_artifacts registered.
- compileall и git diff --check проходят.

Commit:
Implement DNS normalized parser MVP

================================================================================
ЗАДАЧА 19. Реализовать Host normalization parsers MVP
================================================================================

Цель:
Нормализовать ключевые Host текстовые/табличные источники MVP.

Что сделать:
1. Реализовать Host CSV parser for 9-column system event schema.
2. Реализовать JSON-lines metric parser for Metricbeat-like logs.
3. Реализовать raw syslog/auth line parser MVP.
4. Реализовать syscall/txt/sc trace parser MVP with event_index.
5. Missing fields -> NULL.
6. raw_fields_json сохраняет нестандартные поля.

Acceptance criteria:
- Host normalized events write to Parquet.
- event_index работает для streams without timestamp.
- parser_runs and normalized_artifacts registered.
- compileall и git diff --check проходят.

Commit:
Implement Host normalized parser MVP

================================================================================
ЗАДАЧА 20. Реализовать custom parser tasks для PCAP/CAP/PCAPNG/BSON
================================================================================

Цель:
Поддержать сложные форматы без упрощения данных.

Что сделать:
1. Добавить зависимости/tools только если нужны:
   - scapy/pyshark/tshark wrapper for packet parsing
   - bson package for BSON parsing
2. Реализовать packet summary parser:
   - timestamp, src/dst IP, ports, protocol, packet size, TCP flags, DNS fields if decoded.
3. Реализовать BSON parser:
   - descriptor mapping, event documents, args, relative time, event_order.
4. Full payload не сохранять в PostgreSQL.
5. Unknown fields -> raw_fields_json.

Acceptance criteria:
- PCAP/CAP/PCAPNG produce packet/flow normalized summaries.
- BSON produces host behaviour normalized events.
- parser errors are recorded, not destructive.
- compileall и git diff --check проходят.

Commit:
Add custom parsers for packet captures and BSON telemetry

================================================================================
ЗАДАЧА 21. Реализовать LabelResolver
================================================================================

Цель:
Единообразно назначать labels без leakage.

Что сделать:
1. Создать scripts/stage_two/labels/.
2. Реализовать LabelResolver.
3. Поддержать label_mapping_rules from DB/config.
4. Priority:
   embedded label -> ground_truth -> scenario metadata -> filename -> weak IDS alert -> unlabeled.
5. Unknown labels сохранять as label_binary=NULL, label_status='unlabeled'.
6. Не считать unlabeled as benign.

Acceptance criteria:
- label fields populated consistently.
- label_source/status/confidence/rule_id сохранены.
- TEST labels не используются для training decisions.
- compileall и git diff --check проходят.

Commit:
Add canonical label resolver

================================================================================
ЗАДАЧА 22. Реализовать feature artifact contracts
================================================================================

Цель:
Подготовить feature layer для последующего feature engineering.

Что сделать:
1. Создать schemas/features/feature_artifact_v1.json.
2. Описать required traceability fields.
3. Описать feature groups:
   dns_features, host_syscall_features, host_eventlog_features, host_metrics_features,
   network_flow_features, hybrid_features, sequence_features.
4. Создать placeholder writers/registrars without final ML feature extraction.
5. Исключить label/source leakage columns from X contract.

Acceptance criteria:
- Feature schema contract exists.
- feature_artifacts can be registered.
- X exclude list documented.
- compileall и git diff --check проходят.

Commit:
Add feature artifact schema contracts

================================================================================
ЗАДАЧА 23. Реализовать model-ready artifact contracts and preprocessing registry
================================================================================

Цель:
Подготовить model-ready layer without training final models.

Что сделать:
1. Создать schemas/model_ready/model_ready_v1.json.
2. Описать artifacts:
   - X_train.parquet, y_train.parquet
   - X_validation.parquet, y_validation.parquet
   - X_test.parquet, y_test.parquet
   - train_sequences.npz, validation_sequences.npz, test_sequences.npz
   - preprocessing objects
3. Реализовать registry service.
4. Enforce preprocessing fitted_on_role='TRAIN'.

Acceptance criteria:
- model_ready_artifacts registered.
- preprocessing_artifacts linked.
- TEST cannot be fitted_on_role.
- compileall и git diff --check проходят.

Commit:
Add model ready artifact and preprocessing contracts

================================================================================
ЗАДАЧА 24. Реализовать DuckDB analytics layer
================================================================================

Цель:
Выполнять SQL checks over Parquet.

Что сделать:
1. Добавить duckdb dependency.
2. Создать scripts/stage_two/duckdb/.
3. Реализовать DuckDBAnalyticsService.
4. Создать SQL views/scripts:
   - normalized_all
   - features_all
   - model_ready_all
5. Реализовать checks:
   - row counts by role/branch
   - missing required columns
   - split contamination
   - schema mismatch
6. Сохранять reports.

Acceptance criteria:
- DuckDB reads Parquet using read_parquet.
- SQL checks execute without loading data into PostgreSQL.
- Reports saved and registered in data_quality_reports.
- compileall и git diff --check проходят.

Commit:
Add DuckDB analytics checks over Parquet

================================================================================
ЗАДАЧА 25. Реализовать DataQualityChecker and LeakageChecker
================================================================================

Цель:
Блокировать плохие artifacts before downstream stages.

Что сделать:
1. Создать scripts/stage_two/quality/.
2. Реализовать DataQualityChecker.
3. Реализовать LeakageChecker.
4. Проверки:
   - required columns;
   - null counts;
   - duplicate event_uid/sample_uid;
   - role/branch values;
   - label columns absent from X;
   - source_file/scenario_name absent from X;
   - TEST absent from TRAIN artifacts;
   - preprocessing fit only on TRAIN.
5. Сохранять RU/EN report where applicable.

Acceptance criteria:
- Failed leakage check creates CRITICAL report.
- model_ready SUCCESS blocked if leakage check failed.
- compileall и git diff --check проходят.

Commit:
Add Stage Two data quality and leakage checks

================================================================================
ЗАДАЧА 26. Реализовать traceability service
================================================================================

Цель:
Проверять цепочку model_ready -> feature -> normalized -> parser_run -> raw file.

Что сделать:
1. Создать TraceabilityService.
2. Реализовать query by model_ready artifact id/path.
3. Вернуть dataset, file, parser, normalized, feature metadata.
4. Добавить smoke check.

Acceptance criteria:
- Traceability works for registered artifacts.
- Missing link returns explicit error, not silent pass.
- compileall и git diff --check проходят.

Commit:
Add artifact traceability service

================================================================================
ЗАДАЧА 27. Добавить CLI routes для Stage Two
================================================================================

Цель:
Встроить Stage Two services в существующую CLI архитектуру.

Что сделать:
1. Проверить текущие routers manage.py -> router_script -> handlers.
2. Добавить service/action routes without breaking existing Stage One commands.
3. Минимальные actions:
   - stage-two bootstrap-storage
   - stage-two catalog-ingest
   - stage-two seed-parser-registry
   - stage-two normalize-dns
   - stage-two normalize-host
   - stage-two run-duckdb-checks
   - stage-two run-leakage-checks
   - stage-two trace-artifact
4. Обновить manage_commands.

Acceptance criteria:
- Existing commands still work.
- New routes show clear output.
- compileall и git diff --check проходят.

Commit:
Add Stage Two CLI routes

================================================================================
ЗАДАЧА 28. Создать финальную документацию Stage Two в docs/ru и docs/en
================================================================================

Цель:
Задокументировать реализацию для пользователя и дальнейшего Codex work.

Что сделать:
1. Создать docs/ru/normalization/README.md.
2. Создать docs/ru/normalization/storage_architecture.md.
3. Создать docs/ru/normalization/postgresql_catalog_schema.md.
4. Создать docs/ru/normalization/normalized_event_schema.md.
5. Создать docs/ru/normalization/parser_strategy.md.
6. Создать docs/ru/normalization/parquet_duckdb_artifacts.md.
7. Создать docs/ru/normalization/data_quality_checks.md.
8. Создать docs/ru/normalization/data_leakage_prevention.md.
9. Создать английские версии в docs/en/normalization/.

Acceptance criteria:
- RU/EN docs exist.
- Docs match implemented code.
- compileall и git diff --check проходят.

Commit:
Document Stage Two normalization implementation

================================================================================
ЗАДАЧА 29. Провести end-to-end dry run на small sample
================================================================================

Цель:
Проверить весь Stage Two pipeline без обработки полного массива.

Что сделать:
1. Выбрать small sample files from DNS and Host.
2. Запустить bootstrap.
3. Запустить catalog ingestion.
4. Seed parser registry.
5. Normalize sample.
6. Register artifacts.
7. Run DuckDB checks.
8. Run leakage checks.
9. Run traceability.
10. Сохранить report.

Acceptance criteria:
- E2E dry run passes.
- All reports generated.
- Raw files unchanged.
- compileall и git diff --check проходят.

Commit:
Run Stage Two end to end dry run on sample data

================================================================================
ЗАДАЧА 30. Финальная проверка Stage Two readiness
================================================================================

Цель:
Подтвердить готовность перехода к feature engineering/model training.

Что сделать:
1. Проверить DB migrations.
2. Проверить storage paths.
3. Проверить catalog counts.
4. Проверить parser coverage by source_format.
5. Проверить normalized artifacts.
6. Проверить quality/leakage reports.
7. Проверить traceability.
8. Сформировать итоговый RU/EN report.

Acceptance criteria:
- Stage Two readiness report exists.
- Нет CRITICAL leakage issues.
- Все raw files untouched.
- Все artifacts registered.
- compileall и git diff --check проходят.

Commit:
Finalize Stage Two normalization readiness checks
