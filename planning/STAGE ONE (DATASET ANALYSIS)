ЭТАП ПЕРВЫЙ: АНАЛИЗ ДАТАСЕТОВ

Цель этапа:
Подготовить основу для дальнейшей нормализации и приведения датасетов к единому формату данных.

Основные задачи:
- анализ структуры датасетов;
- поиск и выявление битых или повреждённых файлов;
- определение поддерживаемых форматов данных;
- сортировка датасетов по форматам;
- определение пригодных датасетов для дальнейшей обработки;
- подготовка данных для последующего преобразования в единый формат.

Результат этапа:
- сформирован список валидных датасетов;
- выявлены проблемные файлы;
- датасеты распределены по типам и форматам;
- подготовлена база для унификации структуры данных.


# ================= Задача: анализ документации проекта и создание project cheatsheet

# Задача: анализ документации проекта и создание project cheatsheet

## Описание структуры проекта

.env.example      - конфигурация проекта
docs/ru|en        - документация проекта
scripts           - базовые скрипты и обработчики репозитория
manage.py         - основная точка входа

## Цель задачи

Проанализировать основную проектную документацию и подготовить краткую техническую шпаргалку, которая будет использоваться как ориентир для дальнейшей разработки функционального проекта.

## Документы для анализа

Проанализируй следующие файлы:

docs/en/Project Proposal.docx
docs/en/dns_dataset_strategy.md
docs/en/host_datasets_analysis.md

Если для чтения .docx или .md нужны дополнительные инструменты, библиотеки или плагины — подключи их самостоятельно.

## Что нужно сделать

1. Проанализировать текущую структуру репозитория.
2. Изучить указанные документы.
3. Выделить ключевые элементы проекта:
   - основную цель проекта;
   - исследовательскую задачу;
   - архитектуру будущего решения;
   - основные типы датасетов;
   - роли датасетов: TRAIN, VALIDATION, TEST, EXPERIMENTS;
   - ключевые модели и методы;
   - требования к feature engineering;
   - требования к sequence modelling;
   - требования к explainability;
   - ограничения проекта;
   - то, на чём нужно сделать основной упор при разработке.
4. Сформировать краткую шпаргалку для дальнейшей реализации проекта.
5. Создать две версии шпаргалки:
   - на русском языке;
   - на английском языке.

## Куда сохранить результат

Создай файлы:

docs/ru/functional_project_cheatsheet.md
docs/en/functional_project_cheatsheet.md

## Требования к содержанию шпаргалки

Шпаргалка должна быть краткой, структурированной и полезной для разработки.

Обязательные разделы:

1. Project Goal
2. Main Research Focus
3. Core Architecture
4. Dataset Strategy
5. DNS Dataset Roles
6. Host Dataset Roles
7. Feature Engineering Focus
8. Sequence Modelling Focus
9. Explainability Focus
10. Implementation Priorities
11. Project Constraints
12. What Not To Do
13. Recommended Development Order

Для русской версии используй русские заголовки и русский текст.

Для английской версии используй английские заголовки и английский текст.

## Важные ограничения

- Не переписывай всю документацию полностью.
- Не создавай длинный отчёт.
- Не добавляй ML/DL-код.
- Не реализуй обработку датасетов.
- Не изменяй исходные документы.
- Не меняй архитектуру проекта без необходимости.
- Не добавляй новые датасеты, которых нет в документации.
- Не смешивай DNS и host dataset logic.
- Не делай выводы, которых нет в документации.

## Требования к стилю

- Писать кратко и по делу.
- Использовать Markdown.
- Использовать списки и таблицы там, где это улучшает читаемость.
- Формулировки должны быть пригодны для дальнейшей разработки.
- Содержание должно помогать понять, что именно реализовывать в следующих этапах.

## Definition of Done

Задача считается выполненной, если:

- документация была проанализирована;
- создан файл docs/ru/functional_project_cheatsheet.md;
- создан файл docs/en/functional_project_cheatsheet.md;
- обе шпаргалки содержат ключевые выводы по проекту;
- структура проекта не нарушена;
- исходные документы не изменены;
- результат можно использовать как технический ориентир для последующей реализации.


# ============  Задача 1: анализ DNS-датасетов и сохранение структуры в JSON

# Задача 1: анализ DNS-датасетов и сохранение структуры в JSON
## Цель
Реализовать первый этап анализа DNS-датасетов: найти расположение датасетов, определить файлы внутри них, распределить данные по ролям TRAIN, TEST, VALIDATION и сохранить результат в JSON-файлы.
## Контекст проекта
Перед началом работы:
1. Проанализируй текущую структуру репозитория.
2. Не изменяй архитектуру без необходимости.
3. Не реализуй обучение моделей.
4. Не выполняй очистку, преобразование или удаление исходных датасетов.
5. Все временные JSON-файлы должны сохраняться в директорию из переменной PATH_TEMP_DATA.
---
## Переменные окружения
В manage.py должны использоваться следующие переменные:
PATH_DNS_DATASETS
Содержит путь к директории с DNS-датасетами.
PATH_TEMP_DATA
Содержит путь к директории для временного хранения JSON-файлов.
Важно: данные из PATH_TEMP_DATA удалять не нужно. Я буду удалять их вручную, когда они станут не нужны.
---
## JSON-файлы
Нужно создать два JSON-файла.
### 1. dns-path-file.json
Файл должен хранить пути к DNS-датасетам, распределённые по ролям:
{
  "TRAIN": ["path_to_dns_dataset"],
  "TEST": ["path_to_dns_dataset"],
  "VALIDATION": ["path_to_dns_dataset"]
  "EXPERIMENTS": ["path_to_dns_dataset"]
}
### 2. dns-file.json
Файл должен хранить названия файлов внутри DNS-датасетов, распределённые по ролям:
{
  "TRAIN": ["file_name"],
  "TEST": ["file_name"],
  "VALIDATION": ["file_name"]
  "EXPERIMENTS": ["file_name"]
}
Оба файла нужно сохранить в директорию:
PATH_TEMP_DATA
---
## Что нужно реализовать
### 1. Класс для работы с JSON
Создай файл:
scripts/json-data.py
В нём реализуй класс для полноценной работы с JSON-файлами.
Класс должен поддерживать:
- создание JSON-файла;
- чтение JSON-файла;
- запись данных в JSON;
- обновление существующих данных;
- проверку существования JSON-файла;
- создание директории, если она отсутствует;
- корректную работу с UTF-8;
- форматированный вывод JSON через indent.
---
### 2. Скрипт-обработчик DNS-датасетов
Создай отдельный обработчик в директории:
scripts/handlers/
Например:
scripts/handlers/dns_dataset_handler.py
Скрипт должен:
1. Получить путь к DNS-датасетам из PATH_DNS_DATASETS.
2. Просканировать директории DNS-датасетов.
3. Определить пути к датасетам.
4. Определить имена файлов внутри датасетов.
5. Распределить найденные данные по ролям:
   - TRAIN
   - TEST
   - VALIDATION
   - EXPERIMENTS
6. Сохранить результат в:
   - dns-path-file.json
   - dns-file.json
7. Поместить оба JSON-файла в директорию PATH_TEMP_DATA.
---
### 3. Запуск через manage.py
Обнови manage.py, чтобы обработчик запускался через отдельную команду.
Пример команды:
python manage.py module service action
manage.py должен быть только точкой входа. Основная логика должна находиться в scripts/handlers/.
---
## Требования к коду
- Использовать ООП там, где это оправдано.
- Комментарии и docstrings писать на русском языке.
- Код должен быть логически понятным и расширяемым.
- Не смешивать DNS-логику с host dataset logic.
- Не хранить бизнес-логику внутри manage.py.
- Не удалять исходные данные и временные JSON-файлы.
- Не добавлять ML/DL-код.
- Не добавлять лишние зависимости без необходимости.
---
## Отчёт о проделанной работе
После реализации создай два Markdown-отчёта:
report/ru/
report/en/
Названия файлов должны соответствовать выполненной работе.
Например:
report/ru/dns_dataset_analysis_report.md
report/en/dns_dataset_analysis_report.md
Отчёт должен содержать:
- краткое описание выполненной задачи;
- какие файлы были добавлены или изменены;
- какие команды были добавлены в manage.py;
- какие JSON-файлы создаются;
- где сохраняются результаты;
- краткое описание логики работы скрипта.
---
## Git
После реализации задачи напиши commit для этой задачи
-push запрещен, нужен просто текст для commit
---
## Definition of Done
Задача считается выполненной, если:
- создан файл scripts/json-data.py;
- создан DNS-обработчик в scripts/handlers/;
- manage.py запускает анализ DNS-датасетов отдельной командой;
- создаются файлы:
  - dns-path-file.json
  - dns-file.json
- JSON-файлы сохраняются в PATH_TEMP_DATA;
- структура JSON соответствует ролям TRAIN, TEST, VALIDATION, EXPERIMENTS;
- исходные датасеты не изменяются и не удаляются;
- созданы отчёты на русском и английском языках в формате .md;
- код читаемый, модульный и содержит комментарии на русском языке.

Память:
- изучи отчеты о прошлых задачах:
  report/ru

# ============ Задача 2: анализ Host-датасетов и сохранение структуры в JSON
## Цель
Реализовать первый этап анализа Host-датасетов: найти расположение датасетов, определить файлы внутри них, распределить данные по ролям TRAIN, TEST, VALIDATION, EXPERIMENTS и сохранить результат в JSON-файлы.
## Контекст проекта
Перед началом работы:
1. Проанализируй текущую структуру репозитория.
2. Не изменяй архитектуру без необходимости.
3. Не реализуй обучение моделей.
4. Не выполняй очистку, преобразование или удаление исходных датасетов.
5. Все временные JSON-файлы должны сохраняться в директорию из переменной PATH_TEMP_DATA.
---
## Переменные окружения
В manage.py должны использоваться следующие переменные:
PATH_HOST_DATASETS
Содержит путь к директории с Host-датасетами.
PATH_TEMP_DATA
Содержит путь к директории для временного хранения JSON-файлов.
Важно: данные из PATH_TEMP_DATA удалять не нужно. Я буду удалять их вручную, когда они станут не нужны.
---
## JSON-файлы
Нужно создать два JSON-файла.
### 1. host-path-file.json
Файл должен хранить пути к Host-датасетам, распределённые по ролям:
{
  "TRAIN": ["path_to_host_dataset"],
  "TEST": ["path_to_host_dataset"],
  "VALIDATION": ["path_to_host_dataset"],
  "EXPERIMENTS": ["path_to_host_dataset"]
}
### 2. host-file.json
Файл должен хранить названия файлов внутри Host-датасетов, распределённые по ролям:
{
  "TRAIN": ["file_name"],
  "TEST": ["file_name"],
  "VALIDATION": ["file_name"],
  "EXPERIMENTS": ["file_name"]
}

Оба файла нужно сохранить в директорию:
PATH_TEMP_DATA
---
## Что нужно реализовать
### 1. Класс для работы с JSON
Создай файл:
scripts/json-data.py
Если этот файл уже существует после предыдущей задачи, не дублируй его. Проверь текущую реализацию и при необходимости расширь её без нарушения существующей логики.
Класс должен поддерживать:
- создание JSON-файла;
- чтение JSON-файла;
- запись данных в JSON;
- обновление существующих данных;
- проверку существования JSON-файла;
- создание директории, если она отсутствует;
- корректную работу с UTF-8;
- форматированный вывод JSON через indent.
---
### 2. Скрипт-обработчик Host-датасетов
Создай отдельный обработчик в директории:
scripts/handlers/
Например:
scripts/handlers/host_dataset_handler.py
Скрипт должен:
1. Получить путь к Host-датасетам из PATH_HOST_DATASETS.
2. Просканировать директории Host-датасетов.
3. Определить пути к датасетам.
4. Определить имена файлов внутри датасетов.
5. Распределить найденные данные по ролям:
   - TRAIN
   - TEST
   - VALIDATION
   - EXPERIMENTS
6. Сохранить результат в:
   - host-path-file.json
   - host-file.json
7. Поместить оба JSON-файла в директорию PATH_TEMP_DATA.
---
### 3. Запуск через manage.py
Обнови manage.py, чтобы обработчик запускался через отдельную команду.
Формат запуска:
python manage.py module service action
Пример логической команды:
python manage.py host dataset analyze
manage.py должен быть только точкой входа. Основная логика должна находиться в scripts/handlers/.
---
## Требования к коду
- Использовать ООП там, где это оправдано.
- Комментарии и docstrings писать на русском языке.
- Код должен быть логически понятным и расширяемым.
- Не смешивать Host-логику с DNS dataset logic.
- Не хранить бизнес-логику внутри manage.py.
- Не удалять исходные данные и временные JSON-файлы.
- Не добавлять ML/DL-код.
- Не добавлять лишние зависимости без необходимости.
---
## Отчёт о проделанной работе
После реализации создай два Markdown-отчёта:
report/ru/
report/en/
Названия файлов должны соответствовать выполненной работе.
Например:
report/ru/host_dataset_analysis_report.md
report/en/host_dataset_analysis_report.md
Отчёт должен содержать:
- краткое описание выполненной задачи;
- какие файлы были добавлены или изменены;
- какие команды были добавлены в manage.py;
- какие JSON-файлы создаются;
- где сохраняются результаты;
- краткое описание логики работы скрипта.
---
## Git
После реализации задачи напиши commit message для этой задачи.
push запрещён. Нужен только текст commit message.
Пример:
feat: add host dataset structure analysis
---
## Definition of Done
Задача считается выполненной, если:
- создан или переиспользован файл scripts/json-data.py;
- создан Host-обработчик в scripts/handlers/;
- manage.py запускает анализ Host-датасетов отдельной командой;
- создаются файлы:
  - host-path-file.json
  - host-file.json
- JSON-файлы сохраняются в PATH_TEMP_DATA;
- структура JSON соответствует ролям TRAIN, TEST, VALIDATION, EXPERIMENTS;
- исходные датасеты не изменяются и не удаляются;
- созданы отчёты на русском и английском языках в формате .md;
- код читаемый, модульный и содержит комментарии на русском языке;
- подготовлен текст commit message без выполнения push.

# ============ Задача 3: анализ и фильтрация Host-датасетов
## Цель
Реализовать этап анализа и фильтрации Host-датасетов:
исключить ненужные файлы и оставить только те данные, которые подходят для дальнейшего обучения модели.
---
## Контекст проекта
Перед началом работы:
1. Проанализируй текущую структуру репозитория.
2. Не изменяй архитектуру без необходимости.
3. Не реализуй обучение моделей.
4. Не выполняй очистку, преобразование или удаление исходных датасетов.
5. Все временные JSON-файлы должны сохраняться в директорию из переменной PATH_TEMP_DATA.
---
## Переменные окружения
В manage.py должны использоваться следующие переменные:
PATH_HOST_DATASETS
Содержит путь к директории с Host-датасетами.
PATH_TEMP_DATA
Содержит путь к директории для временного хранения JSON-файлов.
Важно:
данные из PATH_TEMP_DATA удалять не нужно.
Удаление будет выполняться вручную.
---
## JSON-файлы
Нужно создать два JSON-файла.
### 1. filter-host-path-file.json
Файл должен хранить пути только к нужным Host-датасетам,
распределённым по ролям:
{
  "TRAIN": ["path_to_host_dataset"],
  "TEST": ["path_to_host_dataset"],
  "VALIDATION": ["path_to_host_dataset"],
  "EXPERIMENTS": ["path_to_host_dataset"]
}
### 2. filter-host-file.json
Файл должен хранить только нужные файлы внутри Host-датасетов,
распределённые по ролям:
{
  "TRAIN": ["file_name"],
  "TEST": ["file_name"],
  "VALIDATION": ["file_name"],
  "EXPERIMENTS": ["file_name"]
}
Оба файла нужно сохранить в директорию:
PATH_TEMP_DATA
---
## Что нужно реализовать
### 1. Анализ и фильтрация Host-датасетов
Изучи следующие файлы:
- docs/ru/project_proposal_analysis.md
- docs/ru/functional_project_cheatsheet.md
- temp_data/host-file.json
- temp_data/host-path-file.json
На основе документации и JSON-файлов:
1. Выполни анализ Host-датасетов.
2. Определи, какие файлы действительно нужны для:
   - обучения модели;
   - sequence modelling;
   - feature engineering;
   - validation;
   - experiments.
3. Исключи ненужные, дублирующиеся или бесполезные файлы.
4. Сохрани только нужные данные в:
   - filter-host-file.json
   - filter-host-path-file.json
---
## Скрипт-обработчик
Создай обработчик в директории:
scripts/handlers/
Например:
scripts/handlers/filter_host_dataset_handler.py
Скрипт должен:
1. Читать:
   - host-file.json
   - host-path-file.json
2. Выполнять фильтрацию файлов.
3. Формировать:
   - filter-host-file.json
   - filter-host-path-file.json
4. Сохранять JSON-файлы в PATH_TEMP_DATA.
5. Записывать исключённые файлы в лог.
---
## Логи
Создай лог-файл:
logs/filter.log
В лог необходимо записывать:
- пути к файлам, исключённым из обучения;
- причину исключения;
- тип датасета;
- роль датасета.
---
## Запуск через manage.py
Обнови manage.py, чтобы фильтрация запускалась отдельной командой.
Пример:
python manage.py host dataset filter
manage.py должен быть только точкой входа.
Основная логика должна находиться в scripts/handlers/.
---
## Требования к коду
- Использовать ООП там, где это оправдано.
- Комментарии и docstrings писать на русском языке.
- Код должен быть логически понятным и расширяемым.
- Не смешивать Host и DNS логику.
- Не хранить бизнес-логику внутри manage.py.
- Не удалять исходные данные.
- Не добавлять ML/DL-код.
- Не добавлять лишние зависимости.
---
## Отчёт о проделанной работе
После реализации создай два Markdown-отчёта:
report/ru/
report/en/
Например:
report/ru/host_dataset_filter_report.md
report/en/host_dataset_filter_report.md
Отчёт должен содержать:
- краткое описание задачи;
- какие файлы были добавлены или изменены;
- какие JSON-файлы создаются;
- какие файлы были исключены;
- краткое описание логики фильтрации;
- описание структуры логов.
---
## Git
После реализации задачи подготовь commit message.
push запрещён.
Нужен только текст commit message.
Пример:
feat: add host dataset filtering system
---
## Definition of Done
Задача считается выполненной, если:
- создан обработчик фильтрации Host-датасетов;
- manage.py запускает фильтрацию отдельной командой;
- создаются:
  - filter-host-file.json
  - filter-host-path-file.json
- JSON-файлы сохраняются в PATH_TEMP_DATA;
- исключённые файлы записываются в logs/filter.log;
- фильтрация выполняется на основе документации проекта;
- исходные датасеты не изменяются;
- созданы отчёты на русском и английском языках;
- код читаемый, модульный и содержит комментарии на русском языке;
- подготовлен commit message без выполнения push.
 "

# ============ Задача 4: сортировка Host-датасетов по форматам
## Цель
Реализовать сортировку Host-датасетов по форматам файлов.
Необходимо проанализировать отфильтрованные Host-датасеты и распределить файлы по форматам в отдельную структуру директорий.
---
## Контекст проекта
Перед началом работы:
1. Проанализируй текущую структуру репозитория.
2. Не изменяй архитектуру без необходимости.
3. Не реализуй обучение моделей.
4. Не выполняй очистку, преобразование или удаление исходных датасетов.
5. Все временные JSON-файлы должны сохраняться в директорию из переменной PATH_TEMP_DATA.
---

## Входные данные
Изучи следующие JSON-файлы:
- filter-host-file.json
- filter-host-path-file.json
Файлы содержат уже отфильтрованные Host-датасеты и список файлов, пригодных для дальнейшего обучения модели.
---
## Основная задача
Необходимо:
1. Проанализировать содержимое:
   - filter-host-file.json
   - filter-host-path-file.json
2. Определить формат каждого файла.
3. Выполнить сортировку файлов по форматам.
4. Сформировать новую структуру директорий в:
PATH_HOST_DATASETS_FILTER
5. Создать внутри директории следующие разделы если их не существует:
   - TRAIN
   - TEST
   - VALIDATION
   - EXPERIMENTS
6. Внутри каждого раздела создать папки по форматам файлов.
Пример структуры:
PATH_HOST_DATASETS_FILTER/
    TRAIN/
        csv/
        json/
        log/
        pcap/
    TEST/
    VALIDATION/
    EXPERIMENTS/
---
## Форматы
- форматы файлов
(csv, txt, GHC, sc, json, log, npz, bson, pcap, pcapng, log.1, log.2, log.3, info, info.1, warn.1, mainlog, mainlog.1, mainlog.2,
mainlog.3, messages, messages.1, journal~, auth.log, cpu.log, diskio.log, filesystem.log, fsstat.log, load.log, memory.log,
network.log, process.log, process.summary.log, service.log, socket.summary.log, syslog.log, uptime.log, pcap.1642084616,
pcap.1642084634, pcap.1642084645, pcap.1642084650, pcap.1642084654, pcap.1642171046, pcap.1642254804, pcap.1642260817
pcap.1642334082, pcap.1642339189, pcap.1642421137, netflow_ids, netflow_day-02, netflow_day-90, wls_day-01,
wls_day-57, wls_day-85
)
---
## Правила сортировки
- Формат определяется по расширению файла.
- Для логов использовать полное смысловое имя:
  - auth.log
  - cpu.log
  - process.summary.log
- Для ротационных логов:
  - log.1 -> log-1
  - mainlog.1 -> mainlog-1
- Для файлов вида:
  - pcap.<timestamp> -> pcap
- Для нестандартных файлов:
  - messages
  - journal~
  - netflow_day-*
  - wls_day-*
использовать отдельные группы по шаблону имени.
---
## Что запрещено
- Не изменять исходные датасеты.
- Не удалять файлы.
- Не перемещать оригинальные данные.
- Не выполнять ML/DL обработку.
- Не смешивать DNS и Host логику.
---
## Скрипт-обработчик
Создай обработчик в:
scripts/handlers/
Например:
scripts/handlers/sort_host_dataset_handler.py
Скрипт должен:
1. Читать:
   - filter-host-file.json
   - filter-host-path-file.json
2. Определять тип файлов.
3. Создавать структуру директорий.
4. Сортировать файлы по форматам.
5. Формировать итоговую структуру внутри:
   - PATH_HOST_DATASETS_FILTER
---
## Запуск через manage.py
Добавь отдельную команду запуска.
Пример:
python manage.py host dataset sort
manage.py должен быть только точкой входа.
Основная логика должна находиться в scripts/handlers/.
---
## Отчёт о проделанной работе
После реализации создай отчёты:
report/ru/
report/en/
Например:
report/ru/host_dataset_sort_report.md
report/en/host_dataset_sort_report.md
Отчёт должен содержать:
- описание задачи;
- какие файлы были добавлены;
- описание структуры сортировки;
- какие форматы поддерживаются;
- пример итоговой структуры директорий;
- описание логики определения форматов.
---
## Git
После реализации задачи подготовь commit message.
push запрещён.
Пример:
feat: add host dataset format sorting system
---
## Definition of Done
Задача считается выполненной, если:
- создан обработчик сортировки Host-датасетов;
- manage.py запускает сортировку отдельной командой;
- создаётся структура:
  - TRAIN
  - TEST
  - VALIDATION
  - EXPERIMENTS
- файлы распределяются по форматам;
- специальные случаи log.1, mainlog.1, pcap.<timestamp> обрабатываются корректно;
- исходные датасеты не изменяются;
- созданы отчёты на русском и английском языках;
- код читаемый и модульный;
- подготовлен commit message без выполнения push.


# ============ Задача 5: запись путей отсортированных Host-датасетов в JSON
## Цель
Реализовать сохранение путей отсортированных Host-датасетов в JSON-файл.
Источник данных:
PATH_HOST_DATASETS_FILTER
Разделы:
- TRAIN
- TEST
- VALIDATION
- EXPERIMENTS
---
## Контекст проекта
Перед началом работы:
1. Проанализируй текущую структуру репозитория.
2. Не изменяй архитектуру без необходимости.
3. Не реализуй обучение моделей.
4. Не выполняй очистку, преобразование или удаление исходных датасетов.
5. Все временные JSON-файлы должны сохраняться в директорию из переменной PATH_TEMP_DATA.
---
## Основная задача
Необходимо:
1. Просканировать структуру:
PATH_HOST_DATASETS_FILTER
2. Определить:
   - раздел датасета:
     - TRAIN
     - TEST
     - VALIDATION
     - EXPERIMENTS
   - формат файлов;
   - пути к файлам.
3. Сформировать JSON-файл:
PATH_TEMP_DATA/sort-path-host-file.json
---
## Структура JSON
Файл должен иметь следующую структуру:
{
  "TRAIN": {
    "format": ["path_to_host_dataset"]
  },
  "TEST": {
    "format": ["path_to_host_dataset"]
  },
  "VALIDATION": {
    "format": ["path_to_host_dataset"]
  },
  "EXPERIMENTS": {
    "format": ["path_to_host_dataset"]
  }
}
---
## Требования к реализации
- Группировать пути по:
  - разделу датасета;
  - формату файлов.
- Поддерживать все ранее обработанные форматы.
- Использовать абсолютные или нормализованные пути.
- Не изменять исходные данные.
- Не удалять файлы.
- Не выполнять ML/DL обработку.
- Не смешивать DNS и Host логику.
---
## Скрипт-обработчик
Создай обработчик в:
scripts/handlers/
Например:
scripts/handlers/save_sort_host_path_handler.py
Скрипт должен:
1. Сканировать PATH_HOST_DATASETS_FILTER.
2. Определять формат файлов.
3. Формировать структуру JSON.
4. Сохранять результат в:
PATH_TEMP_DATA/sort-path-host-file.json
---
## Запуск через manage.py
Добавь отдельную команду запуска.
Пример:
python manage.py host dataset save-paths
manage.py должен быть только точкой входа.
Основная логика должна находиться в scripts/handlers/.
---
## Отчёт о проделанной работе
После реализации создай отчёты:
report/ru/
report/en/
Например:
report/ru/host_dataset_save_paths_report.md
report/en/host_dataset_save_paths_report.md
Отчёт должен содержать:
- описание задачи;
- какие файлы были добавлены или изменены;
- описание структуры JSON;
- описание логики группировки путей;
- пример итогового JSON.
---
## Git
После реализации задачи подготовь commit message.
push запрещён.
Пример:
feat: add sorted host dataset path exporter
---
## Definition of Done
Задача считается выполненной, если:
- создан обработчик сохранения путей;
- manage.py запускает задачу отдельной командой;
- создаётся:
  - PATH_TEMP_DATA/sort-path-host-file.json
- пути сгруппированы по:
  - TRAIN
  - TEST
  - VALIDATION
  - EXPERIMENTS
- внутри разделов данные распределены по форматам;
- исходные данные не изменяются;
- созданы отчёты на русском и английском языках;
- код читаемый, модульный и расширяемый;
- подготовлен commit message без выполнения push.
