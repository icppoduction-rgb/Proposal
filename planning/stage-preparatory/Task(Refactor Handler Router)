Задача: рефакторинг `manage.py` и механизма запуска handlers

Необходимо выполнить рефакторинг `manage.py` и handlers на основе подготовленного плана.

Перед началом реализации:

1. Проанализировать текущую структуру репозитория.
2. Изучить файл с планом рефакторинга:

`C:\Users\fmark\PythonProjects\Proposal\planning\stage-preparatory\HandlerRouterRefactorPlan.md`

Основные требования:

1. Реализовать рефакторинг `manage.py` и handlers согласно плану.
2. Вынести запуск handlers в единый механизм через router.
3. Убрать дублирование логики запуска handlers.
4. Сохранить совместимость с существующими сценариями запуска, если это не противоречит плану рефакторинга.
5. Не изменять бизнес-логику handlers без необходимости.
6. Предусмотреть возможность удобного добавления новых handlers в будущем.

Тестирование:

1. Проверить реализацию на задачах из директории:

`C:\Users\fmark\PythonProjects\Proposal\test_work\tasks`

2. Зафиксировать результаты тестирования в директории:

`C:\Users\fmark\PythonProjects\Proposal\test_work\docs`

Отчётность:

1. Подготовить отчёт о выполненных изменениях.

2. Описать:

   * что было изменено;
   * какие handlers были затронуты;
   * как теперь работает запуск через router;
   * какие команды `manage.py` были изменены или удалены;
   * какие тестовые задачи были выполнены;
   * результаты тестирования;
   * выявленные проблемы или ограничения.

3. Сохранить отчёты в директории:

`C:\Users\fmark\PythonProjects\Proposal\test_work\report`

Ожидаемый результат:

* выполнен рефакторинг `manage.py` и handlers;
* реализован единый запуск handlers через router;
* результаты тестирования сохранены в `test_work\docs`;
* отчёт о проделанной работе сохранён в `test_work\report`.
